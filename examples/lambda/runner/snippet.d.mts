/** The JavaScript the console opens on — ordinary submitted code, not part of the runner's contract. */
export const DEFAULT_SNIPPET: string;
/** A deliberately CPU-bound example: renders a Mandelbrot tile to a PNG. Needs `require` in the runner. */
export const MANDELBROT_SNIPPET: string;
